Точка входа в приложение - объект класса Server, который содержит парамерт app класса FastAPI.
Поставить все зависимости: pip install -r requirements.txt
Запуск приложения: uvicorn app.main:app --reload --port 8080

В каталоге configuration содержутся все конфигурационные файлы для базы данных, приложения и тд

В каталоге repository расположены инструмены для работы с DB, redis и тд

в routing расположены все пути нашего приложения. В папке routes мы создаем модуль каждого "раздела" нашего приложения и далее передаем
сам роутер в кортеж __routers__ в __init__ файле. Далее этот кортеж передается в функцию register_routes, которая передает все пути для нашего FastAPI

В service происходит вся бизнес логика приложения

В shemas находятся все схемы таблиц БД, классов pydantic и тд

main.py - точка входа в прилодение

папка docker нужна для настраивания контейнеров для нашего API, redis, postgres и тд
